= "マージリクエスト ##{@merge_request.iid} は #{@updated_by.name} にクローズされました"

マージリクエストのURL: #{project_merge_request_url(@merge_request.target_project, @merge_request)}

= merge_path_description(@merge_request, 'to')

作成者: #{@merge_request.author_name}
担当者: #{@merge_request.assignee_name}
