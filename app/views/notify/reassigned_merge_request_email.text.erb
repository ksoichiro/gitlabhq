マージリクエスト #<%= @merge_request.iid %> の担当の再割り当て

<%= url_for(project_merge_request_url(@merge_request.target_project, @merge_request)) %>


担当者は <%= "#{@previous_assignee.name} から" if @previous_assignee %> に <%= @merge_request.assignee_name %> 変更されました

