%h3.page-title
  デプロイキー:
  = @key.title
  %small
    = @key.created_at.stamp("2011/08/21")
    に作成
.back-link
  = link_to project_deploy_keys_path(@project) do
    &larr; デプロイキー一覧
%hr
%pre= @key.key
.pull-right
  = link_to '削除', project_deploy_key_path(@project, @key), data: { confirm: '本当に削除しますか？'}, method: :delete, class: "btn-remove btn delete-key"
