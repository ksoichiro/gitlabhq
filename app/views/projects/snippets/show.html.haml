- page_title @snippet.title, "スニペット"
= render "header_title"

%h3.page-title
  = @snippet.title

  .pull-right
    = link_to new_namespace_project_snippet_path(@project.namespace, @project), class: "btn btn-new", title: "新しいスニペット" do
      新しいスニペットを追加

%hr

.append-bottom-20
  .pull-right
    = "##{@snippet.id}"
    %span.light
      by
      = link_to user_path(@snippet.author) do
        = image_tag avatar_icon(@snippet.author_email), class: "avatar avatar-inline s16"
        = @snippet.author_name

  .back-link
    = link_to namespace_project_snippets_path(@project.namespace, @project) do
      &larr; プロジェクトスニペット

.file-holder
  .file-title
    %i.fa.fa-file
    %strong
      = @snippet.file_name
    .file-actions
      .btn-group
        - if can?(current_user, :update_project_snippet, @snippet)
          = link_to "編集", edit_namespace_project_snippet_path(@project.namespace, @project, @snippet), class: "btn btn-sm", title: 'スニペットを編集'
        = link_to "raw", raw_namespace_project_snippet_path(@project.namespace, @project, @snippet), class: "btn btn-sm", target: "_blank"
      - if can?(current_user, :admin_project_snippet, @snippet)
        = link_to "削除", namespace_project_snippet_path(@project.namespace, @project, @snippet), method: :delete, data: { confirm: "本当に削除しますか？" }, class: "btn btn-sm btn-remove", title: 'スニペットを削除'
  = render 'shared/snippets/blob'

%div#notes= render "projects/notes/notes_with_form"
