- page_title "マイルストーン"
= render "header_title"
= render 'shared/milestones_filter'

.gray-content-block
  .pull-right
    - if can? current_user, :admin_milestone, @project
      = link_to new_namespace_project_milestone_path(@project.namespace, @project), class: "pull-right btn btn-new", title: "新しいマイルストーン" do
        %i.fa.fa-plus
        新しいマイルストーン
  .oneline
    Milestone allows you to group issues and set due date for it

.milestones
  %ul.content-list
    = render @milestones

    - if @milestones.blank?
      %li
        .nothing-here-block マイルストーンがありません

  = paginate @milestones, theme: "gitlab"
