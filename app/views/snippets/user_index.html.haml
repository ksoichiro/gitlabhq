- page_title "スニペット", @user.name
%h3.page-title
  = image_tag avatar_icon(@user.email), class: "avatar s24"
  = @user.name
  %span
    \/
    スニペット
  - if current_user
    = link_to new_snippet_path, class: "btn btn-sm add_new pull-right", title: "新しいスニペット" do
      新しいスニペットを追加

%hr

= render 'snippets'
