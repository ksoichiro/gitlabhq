%h3.page-title
  = @snippet.title

  - if @snippet.private?
    %span.label.label-success
      %i.icon-lock
      プライベート

  .pull-right
    = link_to new_snippet_path, class: "btn btn-new btn-small", title: "新しいスニペット" do
      新しいスニペットを追加


.append-bottom-20
  .pull-right
    = "##{@snippet.id}"
    %span.light
      by
      = link_to user_snippets_path(@snippet.author) do
        = image_tag avatar_icon(@snippet.author_email), class: "avatar avatar-inline s16"
        = @snippet.author_name

  .back-link
    - if @snippet.author == current_user
      = link_to user_snippets_path(current_user) do
        &larr; マイスニペット
    - else
      = link_to snippets_path do
        &larr; スニペットを探す

%div= render 'blob'
