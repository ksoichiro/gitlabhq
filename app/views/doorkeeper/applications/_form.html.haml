= form_for application, url: doorkeeper_submit_path(application), html: {class: 'form-horizontal', role: 'form'} do |f|
  - if application.errors.any?
    .alert.alert-danger{"data-alert" => ""}
      %p 入力内容にエラーがあります。確認してください
  = content_tag :div, class: "form-group#{' has-error' if application.errors[:name].present?}" do
    = f.label :name, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :name, '名前', class: 'form-control'
      = doorkeeper_errors_for application, :name
  = content_tag :div, class: "form-group#{' has-error' if application.errors[:redirect_uri].present?}" do
    = f.label :redirect_uri, 'リダイレクトURI', class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_area :redirect_uri, class: 'form-control'
      = doorkeeper_errors_for application, :redirect_uri
      %span.help-block
        1行につき1つのURIを記述してください
      - if Doorkeeper.configuration.native_redirect_uri
        %span.help-block
          ローカルでのテストには
          %code= Doorkeeper.configuration.native_redirect_uri
          を使用してください
  .form-actions
    = f.submit '送信', class: "btn btn-primary wide"
    = link_to "キャンセル", applications_profile_path, class: "btn btn-default"
