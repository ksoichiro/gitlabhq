%h4
  コントリビューションカレンダー
  .pull-right
    %small 課題、マージリクエスト、プッシュイベント
#cal-heatmap.calendar
  :javascript
    new Calendar(
      #{@timestamps.to_json},
      #{@starting_year},
      #{@starting_month},
      '#{user_calendar_activities_path}'
    );
