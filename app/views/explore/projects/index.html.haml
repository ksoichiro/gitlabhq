- page_title    "プロジェクト"
- header_title  "プロジェクト", root_path

- if current_user
  = render 'dashboard/projects_head'
- else
  = render 'explore/head'

.gray-content-block.clearfix
  = render 'filter'
= render 'projects', projects: @projects
= paginate @projects, theme: "gitlab"
