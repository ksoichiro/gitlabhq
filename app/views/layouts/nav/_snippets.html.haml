%ul.nav.nav-sidebar
  - if current_user
    = nav_link(path: user_snippets_path(current_user), html_options: {class: 'home'}) do
      = link_to user_snippets_path(current_user), title: 'マイスニペット', data: {placement: 'right'} do
        = icon('dashboard fw')
        %span
          マイスニペット
  = nav_link(path: snippets_path) do
    = link_to snippets_path, title: 'スニペットを探す', data: {placement: 'right'} do
      = icon('globe fw')
      %span
        スニペットを探す
