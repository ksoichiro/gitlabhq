%ul.nav.nav-sidebar
  = nav_link(controller: :dashboard, html_options: {class: 'home'}) do
    = link_to admin_root_path, title: "統計" do
      = icon('dashboard fw')
      %span
        概要
  = nav_link(controller: [:admin, :projects]) do
    = link_to admin_namespaces_projects_path, title: 'プロジェクト', data: {placement: 'right'} do
      = icon('cube fw')
      %span
        プロジェクト
  = nav_link(controller: :users) do
    = link_to admin_users_path, title: 'ユーザ', data: {placement: 'right'} do
      = icon('user fw')
      %span
        ユーザ
  = nav_link(controller: :groups) do
    = link_to admin_groups_path, title: 'グループ', data: {placement: 'right'} do
      = icon('group fw')
      %span
        グループ
  = nav_link(controller: :deploy_keys) do
    = link_to admin_deploy_keys_path, title: 'デプロイキー', data: {placement: 'right'} do
      = icon('key fw')
      %span
        デプロイキー
  = nav_link do
    = link_to ci_admin_projects_path, title: 'Continuous Integration', data: {placement: 'right'} do
      = icon('building fw')
      %span
        Continuous Integration
  = nav_link(controller: :logs) do
    = link_to admin_logs_path, title: 'ログ', data: {placement: 'right'} do
      = icon('file-text fw')
      %span
        ログ
  = nav_link(controller: :broadcast_messages) do
    = link_to admin_broadcast_messages_path, title: 'メッセージ', data: {placement: 'right'} do
      = icon('bullhorn fw')
      %span
        メッセージ
  = nav_link(controller: :hooks) do
    = link_to admin_hooks_path, title: 'フック', data: {placement: 'right'} do
      = icon('external-link fw')
      %span
        フック
  = nav_link(controller: :background_jobs) do
    = link_to admin_background_jobs_path, title: 'ジョブ', data: {placement: 'right'} do
      = icon('cog fw')
      %span
        ジョブ

  = nav_link(controller: :applications) do
    = link_to admin_applications_path, title: 'アプリケーション', data: {placement: 'right'} do
      = icon('cloud fw')
      %span
        アプリケーション

  = nav_link(controller: :services) do
    = link_to admin_application_settings_services_path, title: 'サービステンプレート', data: {placement: 'right'} do
      = icon('copy fw')
      %span
        サービステンプレート

  = nav_link(controller: :labels) do
    = link_to admin_labels_path, title: 'Labels', data: {placement: 'right'} do
      = icon('tags fw')
      %span
        Labels

  = nav_link(controller: :abuse_reports) do
    = link_to admin_abuse_reports_path, title: "Abuse reports" do
      = icon('exclamation-circle fw')
      %span
        Abuse Reports
        %span.count= AbuseReport.count(:all)

  = nav_link(controller: :application_settings, html_options: { class: 'separate-item'}) do
    = link_to admin_application_settings_path, title: '設定', data: {placement: 'right'} do
      = icon('cogs fw')
      %span
        設定
