!!! 5
%html{ lang: "en"}
  = render "layouts/head", title: "エラー"
  %body{class: "#{app_theme} application"}
    = render "layouts/head_panel", title: "" if current_user
    = render "layouts/flash"
    .container.navless-container
      .error-page
        = yield
