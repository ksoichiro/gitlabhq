%h4
  - if @project
    #{link_to @project.name_with_namespace, @project} プロジェクト内で
  - elsif @group
    #{link_to @group.name, @group} グループ内で
  #{@search_results[:total_results]} 件が見つかりました

%hr

- if @project
  = render "project_results"
- else
  = render "global_results"

:javascript
  $(".search_results .term").highlight("#{escape_javascript(params[:search])}");

