%h3.page-title
  マイルストーン
  %span.pull-right #{@group_milestones.count} 件

%p.light
  %strong #{@group.name}
  グループのマイルストーンだけが表示されています

%hr

= render 'shared/milestones_filter'
.milestones
  .panel.panel-default
    %ul.well-list
      - if @group_milestones.blank?
        %li
          .nothing-here-block マイルストーンがありません
      - else
        - @group_milestones.each do |milestone|
          = render 'milestone', milestone: milestone
  = paginate @group_milestones, theme: "gitlab"
