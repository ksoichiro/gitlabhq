- page_title "マージリクエスト"
- header_title group_title(@group, "マージリクエスト", merge_requests_group_path(@group))

= render 'shared/issuable/filter', type: :merge_requests
.gray-content-block.second-block
  %div
    %strong #{@group.name}
    グループのマージリクエストだけが表示されています。
    - if current_user
      すべてのマージリクエストを確認するには #{link_to 'ダッシュボード', merge_requests_dashboard_path} を表示してください。
.prepend-top-default
  = render 'shared/merge_requests'
