- page_title "Merge Requests"
%h3.page-title
  マージリクエスト

%p.light
  %strong #{@group.name}
  グループのマージリクエストだけが表示されています。
  - if current_user
    すべてのマージリクエストを確認するには #{link_to 'ダッシュボード', merge_requests_dashboard_path} を表示してください。
%hr
.append-bottom-20
  = render 'shared/issuable_filter'
= render 'shared/merge_requests'
